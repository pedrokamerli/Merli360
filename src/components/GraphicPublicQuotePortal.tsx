"use client";

import { FormEvent, useMemo, useState } from "react";
import { CheckCircle2, FileText, Loader2, Upload } from "lucide-react";

type Row = Record<string, any>;
const money = new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" });
const brl = (cents: number) => money.format(Number(cents || 0) / 100);
const shortDate = (value: string) => new Intl.DateTimeFormat("pt-BR", { dateStyle: "short", timeZone: "UTC" }).format(new Date(value));
const status = (value?: string) => ({ PENDING: "Aguardando liberacao", RELEASED: "Preparado para producao", IN_PROGRESS: "Em producao", BLOCKED: "Em verificacao", COMPLETED: "Concluido", SCHEDULED: "Expedicao agendada", DELIVERED: "Entregue", ACCEPTED: "Aceite confirmado", COMPLAINT: "Em atendimento", SENT: "Enviado", VIEWED: "Visualizado", APPROVED: "Aprovado" } as Row)[value || ""] || value || "Aguardando";
const decimal = new Intl.NumberFormat("pt-BR", { maximumFractionDigits: 2 });

function quoteItemMeta(item: Row) {
  const parts = [`${decimal.format(Number(item.quantity || 0))} ${item.unit}`];
  if (item.width && item.height) parts.push(`${decimal.format(Number(item.width))} x ${decimal.format(Number(item.height))} mm`);
  if (item.area) parts.push(`area total ${decimal.format(Number(item.area))} m2`);
  parts.push(`prazo de ${item.deadlineDays || 7} dias`);
  return parts.join(" | ");
}

export function GraphicPublicQuotePortal({ token, quote }: { token: string; quote: Row }) {
  const [saving, setSaving] = useState(false);
  const [message, setMessage] = useState("");
  const [confirmingApproval, setConfirmingApproval] = useState(false);
  const order = quote.orders?.[0];
  const production = order?.productionOrders?.[0];
  const delivery = order?.deliveries?.[0];
  const steps = useMemo(() => production?.steps || [], [production]);
  const delivered = ["DELIVERED", "ACCEPTED"].includes(delivery?.status);
  const orderMessage = delivered ? "Pedido entregue. Obrigado por acompanhar com a gente." : production?.status === "COMPLETED" ? "Producao concluida. A equipe esta preparando a expedicao." : production?.status === "IN_PROGRESS" ? "Seu pedido esta em producao. Acompanhe cada etapa abaixo." : "Orcamento aprovado. Envie a arte para a equipe iniciar a producao.";
  const canUploadArtwork = !delivered && production?.status !== "COMPLETED";
  async function approve() { setSaving(true); const response = await fetch(`/api/gestao-grafica/public-quotes/${token}?action=approve`, { method: "POST" }); const body = await response.json(); setSaving(false); if (!response.ok) return setMessage(body.error || "Nao foi possivel aprovar agora."); setConfirmingApproval(false); window.location.reload(); }
  async function upload(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const file = new FormData(event.currentTarget).get("file"); if (!(file instanceof File) || !file.size) return setMessage("Selecione um arquivo para enviar."); setSaving(true); const response = await fetch(`/api/gestao-grafica/public-quotes/${token}?action=artwork`, { method: "POST", body: new FormData(event.currentTarget) }); const body = await response.json(); setSaving(false); if (!response.ok) return setMessage(body.error || "Nao foi possivel enviar a arte."); setMessage("Arte recebida. A equipe foi avisada."); event.currentTarget.reset(); window.setTimeout(() => window.location.reload(), 800); }
  return <main className="mx-auto max-w-3xl space-y-4 px-4 py-8"><header className="surface-panel p-5"><p className="eyebrow">Acompanhamento do pedido</p><h1 className="mt-1 text-2xl font-black text-slate-950">Orcamento #{quote.number}</h1><p className="mt-2 text-sm font-semibold text-slate-500">{quote.tenant?.brandName || "Merli360"} | Valido ate {shortDate(quote.validUntil)}</p></header>{message ? <p className="rounded-lg bg-emerald-50 p-3 text-sm font-bold text-emerald-800">{message}</p> : null}<section className="surface-panel p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-black text-slate-950">Itens do orcamento</h2><p className="mt-1 text-sm text-slate-500">{quote.paymentTerms || "Condicao a combinar"}</p></div><strong className="text-xl text-slate-950">{brl(quote.totalPriceCents)}</strong></div><div className="mt-4 divide-y rounded-lg border border-slate-200">{quote.items.map((item: Row) => <div key={item.id} className="flex justify-between gap-3 p-3 text-sm"><span><b>{item.description}</b><br /><small>{quoteItemMeta(item)}</small></span><b>{brl(item.priceCents)}</b></div>)}</div>{!order ? <div className="mt-4"><div className="flex flex-wrap gap-2"><button className="primary-action inline-flex items-center gap-2 px-4 py-2" disabled={saving || quote.status === "DRAFT"} type="button" onClick={() => setConfirmingApproval(true)}><CheckCircle2 size={16} />Aprovar orcamento</button><a className="secondary-action inline-flex items-center gap-2 px-4 py-2" href={`/api/gestao-grafica/public-quotes/${token}/pdf`} target="_blank" rel="noreferrer"><FileText size={16} />Baixar PDF</a></div>{confirmingApproval ? <div className="mt-4 border-t border-slate-200 pt-4"><p className="font-black text-slate-950">Confirmar aprovacao de {brl(quote.totalPriceCents)}?</p><p className="mt-1 text-sm font-semibold text-slate-600">Ao confirmar, a equipe recebe o pedido e prepara a ordem de producao.</p><div className="mt-3 flex flex-wrap gap-2"><button className="secondary-action px-4 py-2" type="button" disabled={saving} onClick={() => setConfirmingApproval(false)}>Voltar</button><button className="primary-action inline-flex items-center gap-2 px-4 py-2" type="button" disabled={saving} onClick={approve}>{saving ? <Loader2 className="animate-spin" size={16} /> : <CheckCircle2 size={16} />}Confirmar aprovacao</button></div></div> : null}</div> : null}</section>{order ? <><section className="surface-panel p-5"><h2 className="font-black text-slate-950">Pedido #{order.number}</h2><p className="mt-1 text-sm font-semibold text-emerald-700">{orderMessage}</p>{canUploadArtwork ? <form className="mt-4 flex flex-col gap-2 sm:flex-row" onSubmit={upload}><input className="min-w-0 flex-1 rounded-lg border border-slate-200 px-3 py-2 text-sm" name="file" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" /><button className="primary-action inline-flex items-center justify-center gap-2 px-4 py-2" disabled={saving}><Upload size={16} />Enviar arte</button></form> : null}</section><section className="surface-panel p-5"><div className="flex items-center justify-between gap-3"><h2 className="font-black text-slate-950">Jornada de producao</h2><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-black text-slate-600">{status(production?.status)}</span></div><div className="mt-4 space-y-2">{steps.map((step: Row) => <div key={step.id} className="flex items-center justify-between rounded-lg border border-slate-200 p-3 text-sm"><span className="font-bold text-slate-800">{step.name}</span><span className={step.status === "COMPLETED" ? "font-black text-emerald-700" : "font-black text-slate-500"}>{status(step.status)}</span></div>)}</div>{delivery ? <p className="mt-4 text-sm font-semibold text-slate-600">Entrega: {status(delivery.status)}{delivery.expectedAt ? ` | previsao ${shortDate(delivery.expectedAt)}` : ""}</p> : null}</section></> : null}</main>;
}
